<% content_for :title, "Client Sessions" %>

<h1>Client Sessions</h1>

<div class="pure-g">
  <div class="pure-u-1">
    <table class="pure-table pure-table-striped" style="width: 100%;">
      <thead>
        <tr>
          <th>Client</th>
          <th>Date</th>
          <th>Units</th>
          <th>Description</th>
          <th>Unit Rate</th>
          <th>Session Fee</th>
          <th>Invoice Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if @client_sessions.any? %>
          <% @client_sessions.each do |client_session| %>
            <tr id="<%= dom_id client_session %>">
              <td><%= client_session.client.name %></td>
              <td><%= client_session.session_date.strftime("%d %B %Y") %></td>
              <td><%= client_session.units %></td>
              <td class="description-cell"><%= truncate(client_session.description, length: 50) if client_session.description.present? %></td>
              <td><%= number_to_currency(client_session.unit_session_rate, unit: "£") %></td>
              <td><%= number_to_currency(client_session.fee, unit: "£") %></td>
              <td>
                <% if client_session.invoice %>
                  <span class="status-badge <%= client_session.invoice.status == 'created' ? 'pending' : 'invoiced' %>">
                    <%= client_session.invoice.status.titleize %>
                  </span>
                <% else %>
                  <span class="status-badge unbilled">Unbilled</span>
                <% end %>
              </td>
              <td class="action-buttons">
                <%= link_to client_session, class: "pure-button pure-button-primary button-small" do %>
                  <span class="material-symbols-outlined">subject</span> View
                <% end %>
                <%= render partial: "buttons", locals: { client_session: client_session, button_size: "button-small" } %>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="8" class="is-center">No client sessions found</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<div class="pure-g">
  <div class="pure-u-1">
    <div class="form-buttons">
      <%= link_to new_client_session_path, class: "pure-button pure-button-primary" do %>
        <span class="material-symbols-outlined">add</span> New Client Session
      <% end %>
    </div>
  </div>
</div>

<style>


  .description-cell {
    max-width: 200px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
</style>
